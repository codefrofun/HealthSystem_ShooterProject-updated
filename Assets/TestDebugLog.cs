using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class TestDebugLog : MonoBehaviour
{
    private HealthSystem playerHealth();

    private void Start()
    {
        playerHealth = new HealthSystem();

        DebugLog(playerHealth.ShowHUD())

        private HealthSystem playerHealth();

        private void Start()
        {
            playerHealth = new HealthSystem();

            DebugLog(playerHealth.ShowHUD())
        }
    }
}
*/